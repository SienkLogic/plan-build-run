---
description: "List pending todos and select one to work on"
argument-hint: "[area filter]"
---

This command is provided by the `pbr:todo` skill with subcommand `list`.

Pass arguments as: `list $ARGUMENTS`
