---
description: "Audit milestone completion against original intent before archiving"
argument-hint: "[version]"
---

This command is provided by the `pbr:milestone` skill with subcommand `audit`.

Pass arguments as: `audit $ARGUMENTS`
