---
description: "Archive completed milestone and prepare for next version"
argument-hint: "<version>"
---

This command is provided by the `pbr:milestone` skill with subcommand `complete`.

Pass arguments as: `complete $ARGUMENTS`
