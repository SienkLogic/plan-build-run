#!/usr/bin/env node
'use strict';

const path = require('path');

// Point argv[1] at the real script so its require.main / argv[1] guard fires
const target = path.join(__dirname, '..', 'scripts', 'pbr-tools.js');
process.argv[1] = target;

require(target);
