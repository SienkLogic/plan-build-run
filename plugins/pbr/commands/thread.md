---
description: "Persistent context threads for cross-session work that spans phases. Lighter than pause/resume."
argument-hint: "[name | list | resume <name>]"
---

This command is provided by the `pbr:thread` skill.
