---
description: "Execute a trivial task inline without subagent overhead. No planning, no research — just do it and commit."
argument-hint: "<task description>"
---

This command is provided by the `pbr:fast` skill.
