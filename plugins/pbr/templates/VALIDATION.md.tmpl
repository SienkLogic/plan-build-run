---
phase: {phase_num}
phase_slug: "{slug}"
status: "{status}"
gaps_total: {gaps_total}
gaps_resolved: {gaps_resolved}
gaps_escalated: {gaps_escalated}
created: "{created}"
---

# Phase {phase_num} -- Validation Strategy

> Per-phase Nyquist validation contract for feedback sampling during execution.

## Requirement-to-Test Mapping

| Task ID | Requirement | Test File | Command | Status |
|---------|-------------|-----------|---------|--------|
| {task_id} | {requirement} | {test_file} | {command} | {task_status} |

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | {framework} |
| **Config file** | {config_file} |
| **Quick run command** | `{quick_command}` |
| **Full suite command** | `{full_command}` |
| **Estimated runtime** | ~{runtime}s |

## Sampling Rate

- **After every task commit:** Run `{quick_command}`
- **After every plan wave:** Run `{full_command}`
- **Before `/pbr:review`:** Full suite must be green
- **Max feedback latency:** {max_latency}s

## Wave 0 Requirements

{wave_0_requirements}

*If none: "Existing infrastructure covers all phase requirements."*

## Escalated Gaps

{escalated_gaps}

*If none: "No escalated gaps."*

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| {behavior} | {manual_req} | {reason} | {steps} |

*If none: "All phase behaviors have automated verification."*

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < {max_latency}s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** {approval_status}

<!-- Downstream consumer: plugins/pbr/skills/validate-phase/SKILL.md (creates .planning/phases/{NN}-{slug}/VALIDATION.md) -->
<!-- Downstream consumer: plugins/pbr/agents/nyquist-auditor.md (reads/fills gaps during validation) -->
<!-- Downstream consumer: plugins/pbr/skills/review/SKILL.md (reads validation status) -->
