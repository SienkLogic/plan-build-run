<!-- Downstream consumers:
  - ALL agents: read this file before starting any task. Provides project-specific rules, patterns, and lessons.
  - Executor agent: appends new entries during execution when discovering non-obvious knowledge.
  - Verifier agent: appends entries when verification reveals patterns worth knowing.
  - Debugger agent: appends entries when debugging uncovers root causes applicable to future work.
  - milestone-learnings.js: aggregates SUMMARY patterns into this file at milestone boundaries.
  Breaks if missing: Agents lack project-specific knowledge and may repeat mistakes or reinvent patterns.
-->
# Project Knowledge

> Append-only register of project-specific rules, patterns, and lessons learned.
> ALL agents read this before every task. Add entries when you discover something non-obvious and reusable.
>
> **Capture criteria:** Only add entries that are (1) non-obvious, (2) reusable across phases, and (3) would save future investigation.
> Do NOT add: obvious things, one-off fixes, straightforward implementations.

## Rules

Project-specific rules that ALL agents must follow. Violations cause real problems.

| # | Scope | Rule | Why | Added |
|---|-------|------|-----|-------|
| K001 | {scope} | {rule statement} | {what goes wrong if violated} | {phase/date} |

## Patterns

Reusable approaches discovered during development. Use these instead of reinventing.

| # | Pattern | Where | Notes |
|---|---------|-------|-------|
| P001 | {pattern name} | {files/modules where it's used} | {how to apply it} |

## Lessons Learned

What happened, why, and how to prevent it. Each lesson has a root cause.

| # | What Happened | Root Cause | Fix | Scope |
|---|---------------|------------|-----|-------|
| L001 | {observable problem} | {underlying cause} | {how it was fixed or should be avoided} | {where this applies} |
