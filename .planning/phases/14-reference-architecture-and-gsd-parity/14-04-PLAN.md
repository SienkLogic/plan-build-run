---
phase: "14-reference-architecture-and-gsd-parity"
plan: "14-04"
type: "docs"
wave: 1
depends_on: []
files_modified:
  - "plugins/dev/templates/DEBUG.md.tmpl"
  - "plugins/dev/templates/UAT.md.tmpl"
  - "plugins/dev/templates/discovery.md.tmpl"
  - "plugins/dev/templates/milestone-archive.md.tmpl"
  - "plugins/dev/templates/milestone.md.tmpl"
  - "plugins/dev/templates/continue-here.md.tmpl"
autonomous: true
discovery: 1
must_haves:
  truths:
    - "6 new top-level templates exist in templates/ directory"
    - "Each template uses .md.tmpl extension and <!-- Source: ... --> header convention"
    - "Templates adapt GSD equivalents to Towline terminology and workflow"
  artifacts:
    - "plugins/dev/templates/DEBUG.md.tmpl"
    - "plugins/dev/templates/UAT.md.tmpl"
    - "plugins/dev/templates/discovery.md.tmpl"
    - "plugins/dev/templates/milestone-archive.md.tmpl"
    - "plugins/dev/templates/milestone.md.tmpl"
    - "plugins/dev/templates/continue-here.md.tmpl"
  key_links:
    - "continue-here.md.tmpl consolidates with existing skills/pause/templates/continue-here.md.tmpl"
    - "milestone.md.tmpl and milestone-archive.md.tmpl complement existing skills/milestone/templates/"
    - "DEBUG.md.tmpl aligns with debug skill workflow in skills/debug/SKILL.md"
---

# Plan 14-04: Create New Top-Level Templates

Create 6 new top-level templates in `templates/` for GSD structural parity. These templates define output formats for various Towline workflows. Some overlap with existing skill-local templates (continue-here, milestone) -- in those cases, the top-level template becomes the canonical version.

<task id="14-04-T1" type="auto" tdd="false">
  <name>Create DEBUG, UAT, and discovery templates</name>
  <files>
    plugins/dev/templates/DEBUG.md.tmpl
    plugins/dev/templates/UAT.md.tmpl
    plugins/dev/templates/discovery.md.tmpl
  </files>
  <action>
1. Create `plugins/dev/templates/DEBUG.md.tmpl`:
   - Adapt from GSD `templates/DEBUG.md` (159 lines)
   - Add header: `<!-- Source: templates/DEBUG.md.tmpl | Purpose: Output format for debug session reports -->`
   - Template should define the debug report structure:
     - Frontmatter: session ID, date, phase, issue description, status (investigating/resolved/escalated)
     - Sections: Problem Statement, Investigation Steps, Root Cause, Fix Applied, Verification, Lessons Learned
   - Use `{variable}` placeholders for dynamic content
   - Replace GSD references: `/gsd:debug` -> `/dev:debug`, GSD agent names -> Towline names
   - Read `plugins/dev/skills/debug/SKILL.md` for Towline's debug workflow to ensure template aligns
   - Aim for 100-160 lines

2. Create `plugins/dev/templates/UAT.md.tmpl`:
   - Adapt from GSD `templates/UAT.md` (247 lines)
   - Add header: `<!-- Source: templates/UAT.md.tmpl | Purpose: Output format for user acceptance testing sessions -->`
   - Template should define the UAT report structure:
     - Frontmatter: session date, phase, plan, tester, status (pass/fail/partial)
     - Sections: Test Scope, Test Cases (table with ID, description, expected, actual, status), Issues Found, Overall Assessment
   - Use `{variable}` placeholders
   - Replace GSD-specific references with Towline equivalents
   - Aim for 150-250 lines

3. Create `plugins/dev/templates/discovery.md.tmpl`:
   - Adapt from GSD `templates/discovery.md` (146 lines)
   - Add header: `<!-- Source: templates/discovery.md.tmpl | Purpose: Output format for discovery/research sessions -->`
   - Template should define the discovery report structure:
     - Frontmatter: date, phase, discovery level (0-3), topic
     - Sections: Question, Findings, Recommendations, Confidence, Sources
   - Align with Towline's discovery levels (0=skip, 1=quick, 2=standard, 3=deep) from plan format
   - Use `{variable}` placeholders
   - Aim for 100-150 lines
  </action>
  <verify>
test -f plugins/dev/templates/DEBUG.md.tmpl && echo "OK: DEBUG.md.tmpl exists"
test -f plugins/dev/templates/UAT.md.tmpl && echo "OK: UAT.md.tmpl exists"
test -f plugins/dev/templates/discovery.md.tmpl && echo "OK: discovery.md.tmpl exists"
head -1 plugins/dev/templates/DEBUG.md.tmpl
head -1 plugins/dev/templates/UAT.md.tmpl
head -1 plugins/dev/templates/discovery.md.tmpl
  </verify>
  <done>
DEBUG.md.tmpl, UAT.md.tmpl, and discovery.md.tmpl exist in `templates/` with Towline-adapted content and proper header comments.
  </done>
</task>

<task id="14-04-T2" type="auto" tdd="false">
  <name>Create milestone, milestone-archive, and continue-here templates</name>
  <files>
    plugins/dev/templates/milestone-archive.md.tmpl
    plugins/dev/templates/milestone.md.tmpl
    plugins/dev/templates/continue-here.md.tmpl
  </files>
  <action>
1. Create `plugins/dev/templates/milestone.md.tmpl`:
   - Adapt from GSD `templates/milestone.md` (115 lines)
   - Add header: `<!-- Source: templates/milestone.md.tmpl | Purpose: Output format for milestone completion entries -->`
   - Template should define the milestone entry structure:
     - Frontmatter: milestone name, date, phases covered, status
     - Sections: Goal, Phases Completed (table), Key Metrics (plans executed, tests passing, files changed), Accomplishments, Known Issues
   - Cross-reference: The existing `skills/milestone/templates/stats-file.md.tmpl` (30 lines) covers stats; this template covers the full milestone entry
   - Read `plugins/dev/skills/milestone/SKILL.md` for Towline's milestone workflow to ensure alignment
   - Use `{variable}` placeholders
   - Aim for 80-120 lines

2. Create `plugins/dev/templates/milestone-archive.md.tmpl`:
   - Adapt from GSD `templates/milestone-archive.md` (123 lines)
   - Add header: `<!-- Source: templates/milestone-archive.md.tmpl | Purpose: Output format for milestone archive files -->`
   - Template should define the archive structure:
     - Title and description
     - Table of milestones with: name, date, phases covered, total plans, status
     - Per-milestone summary sections
   - Read `plugins/dev/skills/milestone/SKILL.md` to understand how Towline archives milestones
   - Use `{variable}` placeholders
   - Aim for 80-120 lines

3. Create `plugins/dev/templates/continue-here.md.tmpl`:
   - Read existing `plugins/dev/skills/pause/templates/continue-here.md.tmpl` (71 lines)
   - Create the top-level version at `plugins/dev/templates/continue-here.md.tmpl` with identical or near-identical content
   - Add header: `<!-- Source: templates/continue-here.md.tmpl | Purpose: Output format for pause/resume handoff files -->`
   - The skill-local version (`skills/pause/templates/continue-here.md.tmpl`) remains for now; a future cleanup can redirect the skill to the top-level version
   - Preserve all existing template structure: frontmatter, position info, git status, context summary, next actions
   - Aim for 70-90 lines
  </action>
  <verify>
test -f plugins/dev/templates/milestone.md.tmpl && echo "OK: milestone.md.tmpl exists"
test -f plugins/dev/templates/milestone-archive.md.tmpl && echo "OK: milestone-archive.md.tmpl exists"
test -f plugins/dev/templates/continue-here.md.tmpl && echo "OK: continue-here.md.tmpl exists"
head -1 plugins/dev/templates/milestone.md.tmpl
head -1 plugins/dev/templates/milestone-archive.md.tmpl
head -1 plugins/dev/templates/continue-here.md.tmpl
ls plugins/dev/templates/*.tmpl | wc -l
  </verify>
  <done>
All 6 new top-level templates exist in `templates/`. Total top-level template count is 12 (6 existing + 6 new). The `continue-here.md.tmpl` consolidates the pause/resume handoff format at top level.
  </done>
</task>
