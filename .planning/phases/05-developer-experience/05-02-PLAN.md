---
phase: "05-developer-experience"
plan: "05-02"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - "plan-build-run/skills/quick/SKILL.md"
  - "plan-build-run/skills/config/SKILL.md"
autonomous: true
discovery: 0
gap_closure: false
must_haves:
  truths:
    - "/pbr:quick executes ad-hoc tasks with atomic commits without requiring full plan/review cycle"
    - "/pbr:settings allows toggling all config.json features interactively"
  artifacts:
    - "plan-build-run/skills/quick/SKILL.md: >100 lines"
    - "plan-build-run/skills/config/SKILL.md: >100 lines"
    - "commands/pbr/quick.md exists"
    - "commands/pbr/settings.md exists"
  key_links:
    - "commands/pbr/quick.md routes to pbr:quick skill"
    - "commands/pbr/settings.md routes to pbr:config skill"
    - "quick skill spawns Task(subagent_type: pbr:executor) for execution"
    - "config skill reads and writes .planning/config.json"
provides:
  - "/pbr:quick command with atomic task flow"
  - "/pbr:settings command with interactive config editor"
consumes: []
implements:
  - "REQ-F-013"
  - "REQ-F-015"
---

<task id="05-02-T1" type="auto" complexity="simple">
<name>Verify /pbr:quick skill completeness</name>
<files>
plan-build-run/skills/quick/SKILL.md
commands/pbr/quick.md
</files>
<action>
1. Read `plan-build-run/skills/quick/SKILL.md` and verify it contains:
   - Step 1: Project context check, creates .planning/quick/ if needed
   - Step 2: Task description capture from $ARGUMENTS or user prompt
   - Step 3: Scope validation with AskUserQuestion escalation to full plan
   - Steps 4-6: Slug generation, directory creation, PLAN.md generation (1-3 tasks max)
   - Step 7: Spawns Task(subagent_type: "pbr:executor") with plan file path
   - Step 8: Reads SUMMARY.md results
   - Step 9: Updates STATE.md Quick Tasks table
   - Step 10: Cleans up .active-skill
   - Step 11: Commits planning docs if configured
   - Step 12: Reports results with branded output
   - Composable flags: --discuss, --full
2. Read `commands/pbr/quick.md` and verify it maps to `pbr:quick` skill
3. If any component is missing, add it. Based on current inspection, all components exist — no changes expected.
</action>
<verify>
grep -c "subagent_type" plan-build-run/skills/quick/SKILL.md
grep -c "atomic" plan-build-run/skills/quick/SKILL.md
grep -c "PLAN.md" plan-build-run/skills/quick/SKILL.md
grep -c "pbr:quick" commands/pbr/quick.md
</verify>
<done>/pbr:quick executes ad-hoc tasks with atomic commits, tracking directory, and executor delegation</done>
</task>

<task id="05-02-T2" type="auto" complexity="simple">
<name>Verify /pbr:settings skill completeness</name>
<files>
plan-build-run/skills/config/SKILL.md
commands/pbr/settings.md
</files>
<action>
1. Read `plan-build-run/skills/config/SKILL.md` and verify it contains:
   - Step 1: Loads .planning/config.json, errors if missing
   - Step 1b: Quick Start path (4 essential questions) for first-time users
   - Step 2: Direct argument parsing (depth, model, model-profile, gate, feature, session_phase_limit, git, show)
   - Step 3: Interactive mode with AskUserQuestion for category selection (Depth, Model profile, Features, Git settings, Save as defaults)
   - Step 4: Apply changes and display branded completion output
   - Model profile presets: quality, balanced, budget, adaptive
   - Feature toggle flow with enable/disable AskUserQuestion
   - Version migration (v1 to v2)
   - Save as defaults to ~/.claude/pbr-defaults.json
2. Read `commands/pbr/settings.md` and verify it maps to `pbr:config` skill
3. If any component is missing, add it. Based on current inspection, all components exist — no changes expected.
</action>
<verify>
grep -c "AskUserQuestion" plan-build-run/skills/config/SKILL.md
grep -c "features" plan-build-run/skills/config/SKILL.md
grep -c "Quick Start" plan-build-run/skills/config/SKILL.md
grep -c "pbr:config" commands/pbr/settings.md
</verify>
<done>/pbr:settings allows interactive toggling of all config.json features with Quick Start and Advanced modes</done>
</task>

## Summary

**Plan 05-02**: Verify /pbr:quick and /pbr:settings skills are complete and properly wired

**Tasks:**
1. 05-02-T1: Verify /pbr:quick skill completeness -- plan-build-run/skills/quick/SKILL.md, commands/pbr/quick.md
2. 05-02-T2: Verify /pbr:settings skill completeness -- plan-build-run/skills/config/SKILL.md, commands/pbr/settings.md

**Key files:** plan-build-run/skills/quick/SKILL.md, plan-build-run/skills/config/SKILL.md, commands/pbr/quick.md, commands/pbr/settings.md

**Must-haves:** /pbr:quick atomic task flow with executor delegation, /pbr:settings interactive config editing with all features

**Provides:** /pbr:quick command, /pbr:settings command

**Consumes:** None
